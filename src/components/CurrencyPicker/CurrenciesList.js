import styled from 'styled-components';

const CurrenciesList = styled.ul`
  display: flex;
  flex-wrap: wrap;
  margin: 0.6rem 2rem 4rem 2rem;
  padding: 0;
  list-style: none;
`;

export default CurrenciesList;
